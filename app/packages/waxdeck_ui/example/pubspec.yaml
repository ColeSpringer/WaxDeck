name: waxdeck_ui_catalog
description: "The waxdeck_ui catalog: every component in every theme, plus the full-screen composites the design is signed off against."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  waxdeck_ui:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  alchemist: 0.14.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
