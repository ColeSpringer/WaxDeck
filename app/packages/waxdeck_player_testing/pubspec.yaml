name: waxdeck_player_testing
description: >
  Test companions for waxdeck_player: the deterministic FakeEngine and the
  engine conformance suite. A separate package so waxdeck_player's release
  surface carries no test doubles and no flutter_test dependency; app code
  depends on this only from dev_dependencies.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  waxdeck_player:
    path: ../waxdeck_player

dev_dependencies:
  flutter_lints: ^6.0.0
  # For the fault-table test alone, which feeds the plugin's own
  # exception to the classifier that reads it. Exact, like every pin in
  # waxdeck_player, since the codes are that version's.
  just_audio: 0.10.6
