/// Test companions for the WaxDeck audio engine port: the deterministic
/// [FakeEngine] and the behavioral conformance suite every engine
/// implementation must pass. Import from dev_dependencies only.
library;

export 'src/conformance.dart';
export 'src/fake_engine.dart';
