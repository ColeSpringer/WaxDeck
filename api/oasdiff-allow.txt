# Deliberately accepted breaking changes, matched by `make oasdiff`.
# Temporary by construction: this file rides the change that makes the
# break and is deleted once the base ref contains it.
#
# `EnrichmentPhase` gains `album-art` in the release that adds the
# catalog's album-art backfill. The enum is closed on purpose (the run
# request's `forcePhases` names phases from it), so a new phase is always
# this shape. A client that predates it reads the value as the
# generator's sentinel; nothing in WaxDeck's own app reads `phases`.
GET /library/enrichment added the new 'album-art' enum value to the 'phases/items/' response property for the response status '200'
