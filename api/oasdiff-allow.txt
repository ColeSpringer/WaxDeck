# Deliberately accepted breaking changes, matched by `make oasdiff`.
# Temporary by construction: this file rides the change that makes the
# break and is deleted once the base ref contains it.
#
# `sizeBytes` gains the ceiling the server now enforces. Nothing that
# worked stops working: 16 GiB clears the largest single file anybody
# uploads, and an unbounded declaration was never accepted in practice
# either - it overflowed the quota arithmetic it was checked against.
# Formally a narrowed request schema, which is why it is here.
POST /uploads the 'sizeBytes' request property's max was set to '17179869184.00'
